{% for node in pages_list %}
  {% if group == null or group == node.group %}
    {% if page.url == node.url %}
      <li class="active"><a href="/redmine_git_hosting{{node.url}}" class="active">{{node.title}}</a></li>
    {% else %}
      <li class=""><a href="/redmine_git_hosting{{node.url}}">{{node.title}}</a></li>
    {% endif %}
  {% endif %}
{% endfor %}
{% assign pages_list = nil %}
{% assign group = nil %}
