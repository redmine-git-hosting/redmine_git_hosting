en:

  activerecord:
    errors:
      models:

        project:
          attributes:
            identifier:
              invalid: invalid identifier
              taken: matches existing repository identifier
