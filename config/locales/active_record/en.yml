en:

  module_name: Redmine Git Hosting

  activerecord:
    errors:
      models:

        repository_git_config_key:
          attributes:
            key:
              invalid: "Wrong key format (must contain at least one '.')."

        repository/git:
          attributes:
            base:
              blank_default_exists: "Cannot make repository the default because current default has blank identifier."
            identifier:
              cannot_equal_project: "cannot match identifier of existing project"
              taken: "matches existing repository identifier"
              invalid: invalid
              cannot_change: "cannot be changed"
