<% if not @is_xhr %><h2><%=l(:label_mirror_edit)%></h2><% end %>
<% labelled_tabular_form_for :repository_mirrors, @mirror, :url => { :controller => 'repository_mirrors', :action => 'update', :project_id => @project, :id => @mirror.id }, :html => {:method => :post} do |f| %>
<%= render :partial => 'form', :locals => { :f => f } %>
<%= submit_tag l(:button_save) %>
<% end %>
