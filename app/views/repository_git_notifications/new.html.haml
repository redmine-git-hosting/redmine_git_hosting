= labelled_form_for :repository_git_notification, @git_notification,
                    url:  repository_git_notifications_path(@repository),
                    html: { method: :post, class: 'tabular', remote: @is_xhr } do |f|

  = render partial: 'form', locals: { f: f }
