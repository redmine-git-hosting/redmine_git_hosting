<% if not @is_xhr %>
  <h2><%=l(:label_post_receive_url_create)%></h2>
<% end %>

<% labelled_remote_form_for :repository_post_receive_urls, @prurl,
                            :url  => { :controller => 'repository_post_receive_urls', :action => 'create', :project_id => @project, :id => @prurl.id },
                            :html => { :method => :post, :class => 'tabular' } do |f| %>

  <%= render :partial => 'form', :locals => { :f => f } %>

  <%= submit_tag l(:button_save), :disable_with => l(:label_backup_in_progress) %>

  <% if not @is_xhr %>
    <%= link_to l(:button_cancel), url_for(:controller => 'repositories', :action => 'edit', :id => @repository.id) %>
  <% end %>
<% end %>
