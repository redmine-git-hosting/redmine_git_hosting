begin
  require 'base64'
  require 'digest/md5'
  require 'digest/sha1'
  require 'fileutils'
  #require 'gitolite'
  require 'lockfile'
  require 'net/http'
  require 'net/ssh'
  require 'open3'
  #require 'sidekiq/web'
  #require 'sidekiq-unique-jobs'
  require 'stringio'
  require 'tmpdir'
  require 'uri'
  require 'tmpdir'
  require 'lockfile'
  require 'net/ssh'
rescue
  puts "need to install tmpdir lockfile and net/ssh gems"
end
